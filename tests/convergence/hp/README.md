# hp-convergence

Tests in this directory refine the mesh and increase the representing-space
degree together. The expected rate depends on the refinement strategy and the
regularity distribution of the exact solution. Each future test must document
that strategy explicitly and compare against its corresponding hp estimate.
