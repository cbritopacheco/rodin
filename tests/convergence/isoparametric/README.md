# Isoparametric convergence

Tests in this directory validate approximation on curved domains. Geometry and
field spaces are refined in a controlled way so that the geometry-map error can
be separated from the field error. Future tests must report both the geometry
order and solution-space order and verify the rate predicted by the
isoparametric approximation theory.
